<!DOCTYPE html>
<html lang="en">
<% include partials/head.ejs %>
<body>
    <div class="container">
        <% include partials/header.ejs %>
        <div class="text-center">
            <h2>My Answers:</h2>
        </div>
        <div class="d-flex flex-wrap">
            <% questions.map(function(element) { %>
                <div class="text-center border rounded m-2" style="width: 250px;">
                    <h4 class="m-2"><%= element.ageRangeTitle %></h4>
                    <h6><%= element.question1 %></h6>
                    <p>Their answer which will come from our database</p>
                    <h6><%= element.question2 %></h6>
                    <p>Their answer which will come from our database</p>
                    <h6><%= element.question3 %></h6>
                    <p>Their answer which will come from our database</p>
                </div>
            <% }) %>
        </div>
            <% include partials/footer.ejs %>
    </div>
</body>
</html>